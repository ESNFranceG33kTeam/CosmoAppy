package event

// About the module
const module_name = "event"
const db_name = "events"
const log_name = module_name

// Attendee
const submodule_name_att = module_name + "_attendee"
const db_name_att = module_name + "_attendees"
const log_name_att = submodule_name_att

// Attendee
const submodule_name_sta = module_name + "_staff"
const db_name_sta = module_name + "_staffs"
const log_name_sta = submodule_name_sta

// Statistical vars
const db_name_monthly_stat = "event_stats_monthly"
const log_name_monthly_stat = "event_stat_monthly"
